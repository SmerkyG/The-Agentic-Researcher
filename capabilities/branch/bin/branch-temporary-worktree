#!/usr/bin/env -S uv run --script
# /// script
# dependencies = ["PyYAML>=6.0"]
# ///

from pathlib import Path
import sys

root = Path(__file__).resolve().parents[1]
repo = root.parents[1]
sys.path[:0] = [
    str(root / "package"),
    str(repo / "scripts" / "package"),
]

from agentic_workflows.branch_cli import temporary_worktree_cli

raise SystemExit(temporary_worktree_cli())
