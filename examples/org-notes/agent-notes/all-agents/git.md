# Git Notes

Use this topic note for Git hosting and collaboration conventions that apply
across projects.

Example guidance:

- Prefer small, reviewable commits with clear experiment IDs when applicable.
- Do not force-push shared branches unless the project owner explicitly asks.
- Keep large generated artifacts out of Git; use the configured artifact store
  or scratch location instead.
